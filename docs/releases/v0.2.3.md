# RelayBond v0.2.3 — Exact paid-request replay

Released: September 15, 2026

- Fixes the Agentic Wallet runner so a paid replay carries the exact `symbol` and `scenario` reviewed during quoting.
- Requires those business parameters explicitly on the pay command and refuses contradictory `--scenario` / `--expect` combinations before signing.
- Preserves an unexpected paid delivery as evidence before reporting a status mismatch, preventing a settled response from being discarded.
- Generalizes the independent paid-evidence verifier to validate both `ACCEPTED` and `BREACH` decisions.
- Records transaction `0x438616931bfcca24e7da37a0f3683dbb8db360081a8fb249982d238851595304` honestly as settlement-only evidence: the v0.2.2 runner omitted the stale scenario during replay, so no breach is claimed.
