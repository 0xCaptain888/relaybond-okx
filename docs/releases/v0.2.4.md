# RelayBond v0.2.4 — Onchain service identity parity

Released: September 15, 2026

- Fixes the live rebate claim builder to derive `serviceId` exactly as the deployment path does: `keccak256(utf8("market-data-v1"))`.
- Adds a regression assertion binding the offchain claim to the Solidity/ethers service identifier.
- Prevents a valid paid `BREACH` from targeting an unregistered service slot.
- Publishes the first independently verified paid stale-delivery evidence while leaving the state-changing rebate pending explicit confirmation.
