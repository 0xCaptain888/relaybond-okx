# RelayBond 3:26 submission video

Rendered length: **3 minutes 26 seconds**. Language: English. Narration uses the locally generated Qwen3-TTS `Ryan` neural voice at `0.9×`, selected for a calm, clean American male delivery.

The video shows the real public Demo and public evidence. It does not imply that the read-only Judge Proof creates a new payment or transaction.

## 0:00–0:04 — Cold open

### Screen

```text
Payment: SUCCESS
Response: {}
```

No narration. Hold for four seconds.

## 0:04–0:25.84 — The unfinished task

### Screen

RelayBond hero and Failover Switch logo.

### Voiceover

> An AI agent paid for a service. The payment succeeded, but the provider failed. A refund can return the money. It cannot finish the task. RelayBond closes that gap. The agent can fail. The task still finishes. And the buyer never pays twice.

## 0:25.84–0:43.68 — The reliability clearing layer

### Screen

Live deployment and evidence overview.

### Voiceover

> RelayBond is the reliability clearing layer for paid AI agents. Providers lock USDT zero bonds and sign every delivery. Buyers route work using verified performance, not unmeasurable promises.

## 0:43.68–1:06.08 — One-click Judge Proof

### Screen

Judge Proof ready state.

### Voiceover

> Here is the one-click Judge Proof. It is a read-only verification of a completed live recovery. It does not ask for a wallet signature, and it does not invent a transaction. First, the browser checks the production OKX x402 payment boundary on X Layer Testnet.

## 1:06.08–1:26.64 — Paid Primary breach

### Screen

Primary payment and breach evidence.

### Voiceover

> The OKX Agentic Wallet payment succeeded. But the Primary returned stale data and broke its signed promise. RelayBond verifies the payment, request, provider identity, delivery signature, and breach before recovery begins.

## 1:26.64–1:40.96 — Independent recovery

### Screen

Judge Proof pass state.

### Voiceover

> RelayBond sends the task to an independent bonded backup. It returns a fresh signed result. In the browser, RelayBond verifies both providers, both signatures, and the evidence integrity.

## 1:40.96–1:59.76 — Real X Layer settlement

### Screen

Settlement console and transaction evidence.

### Voiceover

> The buyer does not pay for recovery. X Layer moves one cent of USDT zero from the failed Primary bond to the Backup. The buyer balance stays unchanged, and all seven settlement checks pass.

## 1:59.76–2:18.64 — Portable evidence

### Screen

Settlement console and public verification paths.

### Voiceover

> The final console binds the payment, breach, Backup delivery, signatures, settlement, and buyer-paid-once proof. Anyone can inspect the hashes, call the public endpoint, or repeat the verification in a browser.

## 2:18.64–2:40.88 — Fail closed

### Screen

`RECOVERED` and `FROZEN` paths.

### Voiceover

> RelayBond fails closed. If the Backup also fails, the task becomes frozen instead of fake success. Every state change is tied to evidence, so a second failure can never masquerade as recovery.

## 2:40.88–3:08.32 — Why OKX

### Screen

X Layer contracts, provider registry, OKX.AI Agent and A2MCP context.

### Voiceover

> OKX handles the payment boundary and buyer authorization. X Layer makes provider reliability enforceable capital. RelayBond Agent 13776 exposes the Recovery Proof to other agents through A2MCP.

## 3:08.32–3:25.92 — Close

### Screen

Return to the hero with the public Demo and GitHub context.

### Voiceover

> Payments make agent commerce possible. RelayBond makes it dependable. One payment. One completed task. An independent recovery provider. And evidence anyone can verify.

## Publication checklist

- Upload `artifacts/demo-video/relaybond-okx-dev-day-demo-ryan-final.mp4` as **Unlisted** or **Public**.
- Upload `artifacts/demo-video/relaybond-okx-dev-day-demo-ryan-final.srt` as English captions.
- Do not use either earlier Samantha/Reed/Alex render.
- Do not show secrets, local environment files, wallet credentials or API keys.
